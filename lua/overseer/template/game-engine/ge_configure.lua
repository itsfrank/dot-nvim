return {
    name = "ge configure",
    builder = function(_)
        return {
            name = "ge configure",
            cmd = "stask run ge-configure",
            components = { "default", "unique" },
        }
    end,
}
